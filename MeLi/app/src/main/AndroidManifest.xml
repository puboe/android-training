<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mercadolibre.puboe.meli" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SearchBox"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".SearchResults"
            android:launchMode="singleTop"
            android:label="@string/title_activity_search_results"
            android:parentActivityName="com.mercadolibre.puboe.meli.SearchBox">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.mercadolibre.puboe.meli.SearchBox" />
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>


        </activity>
        <activity
            android:name=".ItemViewActivity"
            android:label="@string/title_activity_item_view"
            android:parentActivityName="com.mercadolibre.puboe.meli.SearchResults">
            <meta-data
                android:name="android.PARENT_ACTIVITY"
                android:value="com.mercadolibre.puboe.meli.SearchResults" />

        </activity>

    </application>

</manifest>
